import { StyleSheet } from 'react-native'

const Style = StyleSheet.create({
    heading: {
        fontSize: 15,
        fontWeight: 'bold',
    },
    error: {
        fontSize: 12,
        color: 'red',
    },
    input: {
        width: '90%',
        borderWidth: 1,
        borderRadius: 5,
        padding: 10,
    },
})
export default Style
